package com.mercadolibre.seguro_autos.utils;

import com.mercadolibre.seguro_autos.dto.VehicleDTO;
import com.mercadolibre.seguro_autos.model.Vehicle;

public class VehicleMapper {
    /*public static Vehicle vehicleDTOToVehicle(VehicleDTO vehicleDTO) {
        return new Vehicle(vehicleDTO.getId(), vehicleDTO.getPatent(), vehicleDTO.getBrand(), vehicleDTO.getModel(),
                           vehicleDTO.getYearProduction(), vehicleDTO.getNumberWheels(), vehicleDTO.getSinisters());
    }*/
}
